spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mutsa_db
    username: root
    password: romance2521*
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true