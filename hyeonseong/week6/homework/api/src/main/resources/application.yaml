spring:
  datasource:
    url: jdbc:mysql://mutsa-db.c7caskqkw2ty.ap-northeast-2.rds.amazonaws.com:3306/mutsa_db
    username: shin
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080
