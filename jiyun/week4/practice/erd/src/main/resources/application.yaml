spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mutsa_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul
    username: root
    password: tcclee8747
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        # 만약 MySQL 8.0 이상을 쓰신다면 아래 줄을 추가하는 게 안전합니다.
        dialect: org.hibernate.dialect.MySQL8Dialect