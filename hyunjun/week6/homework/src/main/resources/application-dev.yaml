spring:
  datasource:
    url: jdbc:mysql://localhost:3307/mutsa_shop_db?characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: admin
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update

