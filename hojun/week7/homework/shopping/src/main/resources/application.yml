spring:
  datasource:
    url: "jdbc:mysql://mutsa-db.cvucu04ea8eu.ap-northeast-2.rds.amazonaws.com:3306/mutsa-db?serverTimezone=Asia/Seoul&useSSL=false&allowPublicKeyRetrieval=true"
    username: admin
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080